<%= simple_form_for @contact do |form| %>
  <%= form.input :name, autofocus: true %>
  <%= form.input :email %>
  <%= form.input :content, as: :text, :input_html => {:class => "span4"} %>
  <%= form.button :submit, 'Summit', class: 'btn-primary' %>
<% end %>
